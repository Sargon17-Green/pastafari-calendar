# Update 8 — Stage 8C: package, artifact and root-checksum audit

## 1. Scope

This is an audit-only Stage 8C rerun aligned to the uploaded repository archive `pastafari-calendar-main (1).zip`. No production/runtime/distribution source file was changed during this stage.

The audit explicitly includes the existing root manifest:

```text
SHA256SUMS.txt
```

## 2. Revision / source archive

- Repository: `Sargon17-Green/pastafari-calendar`
- Branch: `main`
- Package version: `1.3.0`
- Uploaded archive SHA-256: `de65d91ec88b0b5de3b1bff6fd4c7796210fa298e53573f694d81906d8a671bb`
- Git metadata in archive: `not present`
- Commit inferred from committed Stage 8B ledger: `475acbc88c55d07051a67db872cc91a9021c4047`
- Tree inferred from committed Stage 8B ledger: `5b8d587dc48a7916b23fdbc48f85e3cecb3a21ca`

## 3. Checksum audit

### 3.1 Root manifest

```text
sha256sum -c SHA256SUMS.txt
```

Result before adding Stage 8C artifacts: `PASS`.

The root manifest also matched the extracted archive as a complete file inventory: no non-manifest files and no stale manifest entries were found, excluding `SHA256SUMS.txt` itself and the temporary local npm tarball generated during this audit.

Stage 8C adds these root-manifest entries:

```text
./artifacts/update-08-stage-08c-package-audit.json
./artifacts/update-08-stage-08c-report.md
./artifacts/update-08-stage-08c-sha256sums.txt
```

Expected root-manifest entry count after this delta: `773`.

### 3.2 Stage-specific manifests

| Manifest | Result | Note |
|---|---:|---|
| `artifacts/update-08-stage-07-final-sha256sums.txt` | `PASS` | Verified from repository root |
| `artifacts/update-08-stage-08a-sha256sums.txt` | `PASS` | Verified from `artifacts/` because the manifest uses local filenames |
| `artifacts/update-08-stage-08b-sha256sums.txt` | `PASS` | Verified from repository root |
| `artifacts/update-08-stage-08c-sha256sums.txt` | `PASS` | Generated by this stage and verified after creation |

### 3.3 Release checksum script observation

`node scripts/checksums.mjs verify` is not equivalent to raw root-manifest verification in this tree. It rejects many committed `artifacts/*` entries as excluded, while `sha256sum -c SHA256SUMS.txt` passes.

Classification: `FAIL_PREEXISTING_POLICY_MISMATCH`, non-blocking for Stage 8C cryptographic alignment to the existing root manifest, but it remains relevant if Stage 8D intends to require the release script policy gate.

## 4. Package audit

| Check | Result |
|---|---:|
| `npm pack --dry-run --json` | `PASS` |
| `npm pack --json` | `PASS` |
| Dry-run file list equals actual pack file list | `PASS` |
| Tarball SHA-256 | `621cbea5dddd253bbcb740d99e5785bcb7a1a0d35090b7e5dbad92ccb3146daf` |
| Tarball file count | `270` |
| Package size | `90136568` |
| Unpacked size | `120648213` |
| Forbidden path families in tarball (`artifacts/`, `verification/`, `.github/`, `test/`, root `SHA256SUMS.txt`, `package-lock.json`) | `PASS` |

Clean tarball consumer result:

```json
{
  "install": "PASS",
  "import": "PASS",
  "exports": 98,
  "subpathImports": {
    "reverseFindPastafariDate": "function",
    "constraintsSolvePastafariConstraints": "function"
  },
  "canonicalPresentSame": {
    "year": "5000",
    "cutletName": "מחשבה",
    "dayInCutlet": 13,
    "monthName": "חרטה",
    "dayInMonth": 16
  },
  "invalidTodayProviderFailure": "TypeError",
  "postFailureRecovery": "PASS"
}
```

## 5. Leakage scan

Strong local/stage leakage patterns in shipped package content:

```json
{
  "localSandboxPath": 0,
  "stage8ArtifactReference": 0,
  "gitHubWorkflowPath": 7,
  "artifactDirectoryReference": 0
}
```

There were no `/mnt/data`, `/home/oai`, `update-08-stage-08`, `STAGE_8`, `artifacts/update-08`, or `verification/update8` matches inside the shipped package content. `.github/workflows` references appear only in shipped maintenance/documentation scripts and are not classified as Stage 8/local-path leakage.

## 6. Artifact completeness

- Stage 8A artifacts: `PRESENT`
- Stage 8B artifacts: `PRESENT`
- Stage 8C artifacts: `GENERATED_BY_THIS_STAGE`
- Broken literal artifact references in Stage 8A/8B inputs: `4`

## 7. Result

```text
STAGE_8C_RESULT = PACKAGE_AND_ARTIFACT_AUDIT_PASS
READY_FOR_STAGE_8D_FROM_8C = yes
production files changed during Stage 8C: none
root SHA256SUMS.txt updated: yes
```

Non-blocking warning retained for Stage 8D:

```text
CHECKSUM_POLICY_SCRIPT_REJECTS_ARTIFACT_ENTRIES
```

Raw root manifest verification passes. The warning concerns the repository release script policy, not a SHA-256 mismatch.
