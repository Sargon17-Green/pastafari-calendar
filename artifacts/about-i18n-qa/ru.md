# Русский QA — промежуточное состояние всего сайта

## Охват

Проверка охватывает `ru-RU` на всём сайте, а не только `/about/`: основной интерфейс, поиск даты, день действия, сравнение, просмотр года, обратный поиск, ошибки и состояния, руководство пользователя, footer, metadata, manifest и ARIA/доступность.

## Исправления

Отсутствовали четыре ключа контракта:
- `app.brand`
- `reverse.error.limitPositive`
- `reverse.error.limitSafeInteger`
- `reverse.error.absoluteDateField`

`manifest.defaultDescription` был на английском.

Восстановлена потерянная семантика в `search.intro`, `settings.intro`, `guide.1.body`, `guide.4.body`, `guide.5.body` и `guide.6.body`: текущий день Pastafari по умолчанию, активное местоположение наблюдателя, венерианская граница дня из `ASTRONOMICAL-DAY.md`, сброс поиска и дня действия и сохранение выбранного дня действия в последующих поисках.

Queried day унифицирован как `запрошенный день`, queried date — `запрошенная дата`.

## `/about/`

Удалены остатки обычной английской технической прозы: `rejection sampling`, `modulo bias`, `all-day`, смешанные формулировки в разделе Seer, `generic injectivity` и `generic invertibility`. Реальные endpoint-literal сохранены в `code`.

Основные коммиты:
- `2ff49ec883eb41546c8cc630a5bc5511f5eed614`
- `93ee68082b8f239ced62c65b9c512b45d87dba22`

## Проверка

- 258/258 message keys.
- Нет пропущенных или лишних ключей.
- Все наборы `{placeholder}` совпадают с английским контрактом.
- Нет подозрительных смысловых сокращений.
- Совпадения с македонским и болгарским ограничены именами, форматами и законно совпадающими формами; широкого fallback нет.
- В `/about/` ровно 29 stable ID в том же порядке, что и в semantic master, без дубликатов.
- Две таблицы имеют 19 и 9 строк.
- Нет случайного еврейского текста.
- Целевой поиск обычной английской технической прозы чист.
- Обязательные формулы, hash и literal сохранены, включая `Q=2^{127}-1`, `R=\\operatorname{SAVE}(S+149r)`, `47\\times123=5781`, `5781-5778=3`, `RRULE:FREQ=YEARLY`, `F(c+T,t+T)=F(c,t)`, `14{,}777{,}149` и `8e155fa4198ea7bcfeb16138ac5d6662706f4d93`.

## Открытые gates

Этот файл **не доказывает**, что весь сайт был проверен в отдельной LLM-сессии, разговор в которой полностью вёлся по-русски. Обязательный `linguistic QA` всё ещё открыт.

Также не завершены реальный render QA на desktop и 390 px mobile, accessibility, PWA/offline и language switching.

## Статус

Текст, UI и семантический контракт готовы к следующему gate. Текущий корректный статус — **semantic QA**, а не `linguistic QA`.
