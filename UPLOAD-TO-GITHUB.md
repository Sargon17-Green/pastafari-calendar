# העלאה דרך אתר GitHub

1. פתח את המאגר ובחר `Add file` ואז `Upload files`.
2. חלץ את קובץ ה־ZIP במחשב. אל תעלה את ה־ZIP עצמו.
3. היכנס לתיקייה `browser` במאגר, או צור אותה אם אינה קיימת.
4. גרור לחלון ההעלאה את ששת הקבצים שבתיקיית `browser` שחולצה.
5. אשר את ה־commit.

אין אף קובץ מעל 25 MiB. שני קובצי הליבה הגדולים הם בערך 14.8MB כל אחד.

לאחר ההעלאה אפשר לקבע גרסה באמצעות תגית `v1.1.0`, או להחליף זמנית את `@v1.1.0` ב־`@main` בכתובת jsDelivr.
